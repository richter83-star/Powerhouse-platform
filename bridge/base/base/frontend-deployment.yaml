#! frontend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: frontend
    namespace: powerhouse-platform
    labels:
        com.docker.compose.project: powerhouse-platform
        com.docker.compose.service: frontend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: powerhouse-platform
            com.docker.compose.service: frontend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: powerhouse-platform
                com.docker.compose.service: frontend
                com.docker.compose.network.default: "true"
        spec:
            restartPolicy: Always
            containers:
                - name: powerhouse-frontend
                  image: powerhouse-platform-frontend:latest
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: BACKEND_INTERNAL_URL
                      value: "http://backend:8000"
                    - name: DATABASE_URL
                      valueFrom:
                        secretKeyRef:
                            name: powerhouse-secrets
                            key: DATABASE_URL
                    - name: NEXTAUTH_SECRET
                      valueFrom:
                        secretKeyRef:
                            name: powerhouse-secrets
                            key: NEXTAUTH_SECRET
                    - name: NEXTAUTH_URL
                      value: "http://localhost:3000"
                    - name: NEXT_PUBLIC_API_URL
                      value: "http://localhost:8001"
                    - name: NODE_ENV
                      value: "production"
                    - name: POSTGRES_DB
                      valueFrom:
                        secretKeyRef:
                            name: powerhouse-secrets
                            key: POSTGRES_DB
                    - name: POSTGRES_PASSWORD
                      valueFrom:
                        secretKeyRef:
                            name: powerhouse-secrets
                            key: POSTGRES_PASSWORD
                    - name: POSTGRES_USER
                      valueFrom:
                        secretKeyRef:
                            name: powerhouse-secrets
                            key: POSTGRES_USER
                  ports:
                    - name: frontend-3000
                      containerPort: 3000
