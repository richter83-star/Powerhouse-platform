# Core FastAPI dependencies - COMPATIBLE VERSIONS
fastapi==0.118.0  # Compatible with google-adk
uvicorn[standard]>=0.34.0
pydantic>=2.11.0
pydantic-settings>=2.6.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<4.0.0  # Compatible with passlib 1.7.4 (bcrypt 4.0.0+ removed __about__ module)
python-multipart>=0.0.9
PyJWT[crypto]>=2.10.1
cryptography==41.0.7

# Database (Windows-compatible versions)
sqlalchemy==2.0.23
psycopg2-binary>=2.9.10  # Updated for Windows compatibility
alembic==1.12.1

# Utilities
python-dotenv==1.0.0
httpx>=0.27.2
aiofiles>=24.1.0
# Async HTTP client for CI/CD and external integrations
aiohttp>=3.9.0

# Monitoring and logging
python-json-logger==2.0.7

# Kafka for real-time feedback pipeline (Windows-compatible)
kafka-python>=2.0.2

# Machine Learning
numpy>=1.24.0
scikit-learn>=1.3.0  # For ML models and preprocessing
torch>=2.0.0  # PyTorch for neural networks (optional but recommended)

# Distributed Systems & Message Queue
redis==5.0.1
nats-py==2.6.0

# Observability & Telemetry - COMPATIBLE VERSIONS
opentelemetry-api==1.37.0  # Required by google-adk
opentelemetry-sdk==1.37.0  # Must match API version
opentelemetry-instrumentation-fastapi==0.58b0  # Compatible with SDK 1.37.0
opentelemetry-instrumentation-asgi==0.58b0
opentelemetry-instrumentation-requests==0.58b0
opentelemetry-exporter-prometheus>=0.42b0
opentelemetry-semantic-conventions==0.58b0  # Must match instrumentation versions
opentelemetry-util-http==0.58b0
prometheus-client==0.19.0

# Monitoring
psutil==5.9.6  # Already in requirements, but ensuring it's here for metrics

# State Management
cloudpickle==3.0.0

# System Monitoring
psutil==5.9.6

# LLM Providers
openai>=1.0.0
anthropic>=0.37.0
tenacity>=8.2.3

# HTTP client for RouteLLM provider
requests>=2.31.0

# Email Service
sendgrid>=6.10.0  # SendGrid email service
boto3>=1.34.0  # AWS SES support (optional)

# Payment Processing
stripe>=7.0.0  # Stripe API for payment processing and subscriptions

# SAML Authentication (optional, for enterprise SSO)
python3-saml>=1.15.0  # SAML 2.0 authentication library

# OAuth Authentication (optional, for SSO)
authlib>=1.2.0  # OAuth 2.0 and OpenID Connect library

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
httpx>=0.27.2  # For TestClient

# Causal Reasoning
networkx>=3.0
pgmpy>=0.1.19  # Optional, for advanced causal discovery
pandas>=2.0.0  # For pgmpy data handling

# Swarm Intelligence
scipy>=1.10.0  # Optimization algorithms

# Program Synthesis
restrictedpython>=5.0  # Optional, for safer code execution

# Multi-Modal Learning
transformers>=4.30.0  # Vision-language models
Pillow>=10.0.0  # Image processing
openai-whisper>=20230314  # Audio processing (optional)
